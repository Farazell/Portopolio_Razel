{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome",
            "url": "http://localhost:8000", // <- Sesuaikan dengan port local server proyek Anda (misal Laravel: 8000, Vite: 5173)
            "webRoot": "${workspaceFolder}",
        }
    ]
}